services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: propertly
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - propertly_data:/var/lib/postgresql/data

volumes:
  propertly_data:
